// vi: spell spl=en


// Universe
// Universe object has all planets as properties. (name is the key of the planet)
// This makes it easy to access planets

// Planet list
// An array with all planets, so it's easy to iterate over all planets.

function Planet( key, name, x, y, resources ) {
    // Galaxy related 
    self.key  = key;
    self.name = name;
    self.x = x;
    self.y = y;
    self.resources = resources;

    // Viewer related
}


